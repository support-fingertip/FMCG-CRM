<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Territory_Required_When_Active</fullName>
    <active>true</active>
    <description>Active accounts must have a Territory so downstream records (orders, visits, price lists, targets) inherit it cleanly.</description>
    <errorConditionFormula>AND(Is_Active__c, ISBLANK(Territory__c))</errorConditionFormula>
    <errorDisplayField>Territory__c</errorDisplayField>
    <errorMessage>Active accounts must be assigned to a territory.</errorMessage>
</ValidationRule>
